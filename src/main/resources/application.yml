spring:
  application:
    name: TunaMelt

  datasource:
    url: jdbc:postgresql://localhost:5432/tunamelt
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true